server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # Fall back to index.html so React Router can handle client-side routes after refreshes.
    try_files $uri $uri/ /index.html;
  }
}
